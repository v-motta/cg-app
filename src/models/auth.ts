export type LoginInputs = {
  username: string;
  password: string;
  remember: boolean;
};

export type RegisterInputs = {
  name: string;
  username: string;
  email: string;
  password: string;
};
